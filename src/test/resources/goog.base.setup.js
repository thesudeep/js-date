window.CLOSURE_NO_DEPS = true;

/*
var goog = {
    basePath: "${project.build.directory}${file.separator}javascriptFramework${file.separator}closure-library${file.separator}closure${file.separator}goog"
};
*/
